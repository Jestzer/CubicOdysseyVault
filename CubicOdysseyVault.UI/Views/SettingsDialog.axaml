<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:CubicOdysseyVault.UI.ViewModels"
        x:Class="CubicOdysseyVault.UI.Views.SettingsDialog"
        x:DataType="vm:SettingsViewModel"
        Title="Settings"
        Icon="/Assets/icon.png"
        Width="560" Height="700"
        MinWidth="520" MinHeight="600"
        CanResize="False"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource MainBackgroundBrush}">

    <DockPanel Margin="20">
        <!-- Buttons (bottom) -->
        <StackPanel DockPanel.Dock="Bottom"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="{StaticResource SpaceSm}"
                    Margin="0,16,0,0">
            <Button Classes="secondary" Content="Cancel" MinWidth="90" Command="{Binding CancelCommand}" />
            <Button Classes="primary"   Content="Save"   MinWidth="90" Command="{Binding SaveCommand}" />
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Spacing="{StaticResource SpaceLg}">
                <!-- Backup root -->
                <StackPanel Spacing="6">
                    <TextBlock Classes="label" Text="BACKUP ROOT" />
                    <TextBlock Classes="body-sm"
                               Text="Snapshots are written here. Defaults to a folder under your local app data."
                               TextWrapping="Wrap" />
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Classes="secondary"
                                Content="Browse..."
                                MinWidth="90"
                                Margin="8,0,0,0"
                                Command="{Binding BrowseBackupRootCommand}" />
                        <TextBox Text="{Binding BackupRootPath}" />
                    </DockPanel>
                </StackPanel>

                <!-- Game install path -->
                <StackPanel Spacing="6">
                    <TextBlock Classes="label" Text="GAME INSTALL PATH" />
                    <TextBlock Classes="body-sm"
                               Text="Path to the folder containing data/configs/items/ and data/sprites/icons.png. Used for friendly item titles and per-item icons in the save inspector. Leave blank to auto-discover under your Steam libraries."
                               TextWrapping="Wrap" />
                    <DockPanel>
                        <Button DockPanel.Dock="Right"
                                Classes="secondary"
                                Content="Browse..."
                                MinWidth="90"
                                Margin="8,0,0,0"
                                Command="{Binding BrowseGameInstallCommand}" />
                        <TextBox Text="{Binding GameInstallPath}"
                                 Watermark="(auto-discover)" />
                    </DockPanel>
                </StackPanel>

                <!-- Manual sources -->
                <StackPanel Spacing="6">
                    <TextBlock Classes="label" Text="MANUAL SAVE SOURCES" />
                    <TextBlock Classes="body-sm"
                               Text="Extra folders to scan for slots — useful for external drives or recovered backups."
                               TextWrapping="Wrap" />
                    <Border Classes="card"
                            Padding="0">
                        <ListBox Height="100"
                                 ItemsSource="{Binding ManualSourceRoots}"
                                 SelectedItem="{Binding SelectedManualRoot}"
                                 Background="Transparent"
                                 BorderThickness="0">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Classes="body"
                                               Text="{Binding}" />
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </Border>
                    <StackPanel Orientation="Horizontal" Spacing="{StaticResource SpaceSm}">
                        <Button Classes="secondary" Content="Add..." Command="{Binding AddManualRootCommand}" />
                        <Button Classes="secondary"
                                Content="Remove"
                                Command="{Binding RemoveManualRootCommand}"
                                IsEnabled="{Binding SelectedManualRoot, Converter={x:Static ObjectConverters.IsNotNull}}" />
                    </StackPanel>
                </StackPanel>

                <!-- Watcher -->
                <StackPanel Spacing="6">
                    <TextBlock Classes="label" Text="FILE WATCHER" />
                    <CheckBox Content="Watch save folders and snapshot on change"
                              IsChecked="{Binding WatcherEnabled}"
                              Foreground="{StaticResource TextPrimaryBrush}" />
                    <DockPanel IsEnabled="{Binding WatcherEnabled}">
                        <TextBlock DockPanel.Dock="Left"
                                   Classes="body"
                                   Text="Debounce window (seconds):"
                                   VerticalAlignment="Center" />
                        <NumericUpDown Value="{Binding WatcherDebounceSeconds}"
                                       Minimum="1" Maximum="300"
                                       FormatString="0"
                                       Width="120"
                                       Margin="12,0,0,0"
                                       HorizontalAlignment="Left" />
                    </DockPanel>
                </StackPanel>

                <!-- Retention -->
                <StackPanel Spacing="6">
                    <TextBlock Classes="label" Text="RETENTION" />
                    <TextBlock Classes="body-sm"
                               Text="How many automatic snapshots to keep at each tier. Manual + tagged snapshots are kept forever."
                               TextWrapping="Wrap" />
                    <Grid ColumnDefinitions="120,*" RowDefinitions="Auto,8,Auto,8,Auto">
                        <TextBlock Grid.Row="0" Grid.Column="0" Classes="body" Text="Hourly:"
                                   VerticalAlignment="Center" />
                        <NumericUpDown Grid.Row="0" Grid.Column="1"
                                       Value="{Binding HourlySnapshotsKept}"
                                       Minimum="0" Maximum="999"
                                       FormatString="0"
                                       Width="120" HorizontalAlignment="Left" />
                        <TextBlock Grid.Row="2" Grid.Column="0" Classes="body" Text="Daily:"
                                   VerticalAlignment="Center" />
                        <NumericUpDown Grid.Row="2" Grid.Column="1"
                                       Value="{Binding DailySnapshotsKept}"
                                       Minimum="0" Maximum="999"
                                       FormatString="0"
                                       Width="120" HorizontalAlignment="Left" />
                        <TextBlock Grid.Row="4" Grid.Column="0" Classes="body" Text="Weekly:"
                                   VerticalAlignment="Center" />
                        <NumericUpDown Grid.Row="4" Grid.Column="1"
                                       Value="{Binding WeeklySnapshotsKept}"
                                       Minimum="0" Maximum="999"
                                       FormatString="0"
                                       Width="120" HorizontalAlignment="Left" />
                    </Grid>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</Window>
